import 'package:flutter/cupertino.dart';

class WStatusList extends StatelessWidget{

  WStatusList();

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Column(
        mainAxisAlignment: MainAxisAlignment.center,
        children: <Widget>[

        ],
      ),
    );
  }


}